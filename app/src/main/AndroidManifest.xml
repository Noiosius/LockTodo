<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_lock_todo"
        android:label="Lock Todo"
        android:supportsRtl="true"
        android:theme="@style/Theme.LockTodo">

        <activity
            android:name=".SettingsActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".QuickTodoActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:showWhenLocked="true"
            android:theme="@style/Theme.LockTodo.Dialog" />

        <activity
            android:name=".ReorderActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:noHistory="true"
            android:showWhenLocked="true"
            android:theme="@style/Theme.LockTodo.Dialog" />

        <receiver
            android:name=".LockTodoWidget"
            android:exported="true"
            android:label="LockTodo">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/lock_todo_widget_info" />
        </receiver>

    </application>
</manifest>
