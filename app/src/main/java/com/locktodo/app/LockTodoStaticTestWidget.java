package com.locktodo.app;

import android.appwidget.AppWidgetProvider;

/**
 * Minimal fixed-size AppWidget provider used to test Samsung One UI / LockStar hosting.
 * Intentionally contains no update logic, PendingIntent, dynamic RemoteViews, or resize behavior.
 */
public class LockTodoStaticTestWidget extends AppWidgetProvider {
}
